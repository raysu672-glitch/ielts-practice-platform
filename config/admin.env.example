# 管理员初始密码（仅在新库首次插入 admin 行时使用；已有库不会被覆盖）
# 复制为 config/admin.env 后填写；admin.env 不提交 GitHub。
#
# 本地开发也可直接设置环境变量：
#   PowerShell: $env:IELTS_ADMIN_PASSWORD="你的密码"
#   Linux:      export IELTS_ADMIN_PASSWORD='你的密码'

IELTS_ADMIN_PASSWORD=your_admin_password_here
